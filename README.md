# IHoover

Pour lancer le projet ouvrez un terminal est lancer la commande `npm install` suivie de la commande `npm run start`.

Enfin vous pouvez acceder à l'application sur un navigateur à l'adresse http://localhost:4200/

Bonne journée à vous !
